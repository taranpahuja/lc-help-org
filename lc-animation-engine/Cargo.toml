[package]
name = "lc-animation-engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"] # Crucial for WebAssembly compilation

[dependencies]
wasm-bindgen = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
